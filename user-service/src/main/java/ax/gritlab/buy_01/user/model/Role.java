package ax.gritlab.buy_01.user.model;

/**
 * User roles in the system.
 */
public enum Role {
    /** Client role with buyer permissions. */
    CLIENT,
    /** Seller role with product management permissions. */
    SELLER
}

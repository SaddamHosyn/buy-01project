pipeline {
    agent any

    parameters {
        choice(name: 'DEPLOY_ENV', choices: ['local', 'staging', 'production'], description: 'Select deployment environment')
        booleanParam(name: 'SKIP_TESTS', defaultValue: false, description: 'Skip tests (use with caution)')
        booleanParam(name: 'FORCE_DEPLOY', defaultValue: false, description: 'Force deployment even if tests fail')
    }

    environment {
        DOCKER_USER          = credentials('docker-hub-username') // Configurable from Jenkins credentials
        REGISTRY_CREDENTIALS = 'docker-hub-credentials'
        BUILD_VERSION        = "${env.BUILD_NUMBER}"
        PREVIOUS_BUILD       = "${env.BUILD_NUMBER.toInteger() - 1}"
    }

    // ⚡ WEBHOOK TRIGGER (Instant builds - webhook configured!)
    triggers {
        githubPush()
    }

    stages {

        stage('Checkout') {
            steps {
                echo "📦 Checking out code..."
                checkout scm
            }
        }

        stage('Secret Scanning') {
            steps {
                echo '🔍 Scanning for accidentally committed secrets...'
                script {
                    def secretsFound = false
                    
                    // AWS Access Key pattern - exclude deployment dir and Jenkinsfile
                    def result1 = sh(script: "grep -r -E -i 'AWS.*ACCESS.*KEY.*=.*[A-Z0-9]{20}' --include='*.java' --include='*.js' --include='*.ts' --include='*.yml' --include='*.properties' --exclude-dir=deployment --exclude-dir=target --exclude-dir=node_modules . || true", returnStdout: true).trim()
                    if (result1) {
                        echo "⚠️ Potential AWS Access Key found"
                        secretsFound = true
                    }
                    
                    // AWS Secret Key pattern - exclude deployment dir
                    def result2 = sh(script: "grep -r -E -i 'AWS.*SECRET.*KEY.*=.*[A-Za-z0-9+/]{40}' --include='*.java' --include='*.js' --include='*.ts' --include='*.yml' --include='*.properties' --exclude-dir=deployment --exclude-dir=target --exclude-dir=node_modules . || true", returnStdout: true).trim()
                    if (result2) {
                        echo "⚠️ Potential AWS Secret Key found"
                        secretsFound = true
                    }
                    
                    // API Key pattern - only match if followed by actual value (30+ chars)
                    def result3 = sh(script: "grep -r -E -i 'api.?key.*=.*[A-Za-z0-9]{30,}' --include='*.java' --include='*.js' --include='*.ts' --include='*.yml' --include='*.properties' --exclude-dir=deployment --exclude-dir=target --exclude-dir=node_modules . || true", returnStdout: true).trim()
                    if (result3) {
                        echo "⚠️ Potential API Key found"
                        secretsFound = true
                    }
                    
                    // Private Key pattern - only in actual key files
                    def result4 = sh(script: "grep -r 'BEGIN.*PRIVATE KEY' --include='*.pem' --include='*.key' --exclude-dir=deployment --exclude-dir=target --exclude-dir=node_modules . || true", returnStdout: true).trim()
                    if (result4) {
                        echo "⚠️ Potential Private Key found"
                        secretsFound = true
                    }
                    
                    if (secretsFound) {
                        error('❌ Secrets detected in code! Please remove them and use Jenkins credentials.')
                    } else {
                        echo '✅ No secrets detected in code.'
                    }
                }
            }
        }

        stage('Backend Tests') {
    when {
        expression { params.SKIP_TESTS == false }
    }
    steps {
        echo '🧪 Running backend tests...'
        script {
            dir('user-service') {
                sh 'mvn clean test -Dmaven.test.failure.ignore=false'
            }
            dir('product-service') {
                sh 'mvn clean test -Dmaven.test.failure.ignore=false'
            }
            dir('media-service') {
                sh 'mvn clean test -Dmaven.test.failure.ignore=false'
            }
        }
    }
    post {
        always {
            junit allowEmptyResults: true, testResults: '**/target/surefire-reports/*.xml'
            // Archive test coverage reports
            archiveArtifacts artifacts: '**/target/surefire-reports/**, **/target/site/jacoco/**', allowEmptyArchive: true
        }
        failure {
            echo '❌ Backend tests failed!'
        }
        success {
            echo '✅ Backend tests passed!'
        }
    }
}

        stage('Frontend Tests') {
    when {
        expression { params.SKIP_TESTS == false }
    }
    steps {
        echo '🧪 Running frontend tests (Karma/Jasmine)...'
        dir('buy-01-ui') {
            sh '''
                export CHROME_BIN=/usr/bin/chromium
                rm -rf node_modules package-lock.json
                npm install --legacy-peer-deps
                npx ng test --watch=false --browsers=ChromeHeadlessCI --code-coverage
            '''
        }
    }
    post {
        always {
            // Archive frontend test coverage
            archiveArtifacts artifacts: 'buy-01-ui/coverage/**', allowEmptyArchive: true
        }
        failure {
            echo '❌ Frontend tests failed!'
        }
        success {
            echo '✅ Frontend tests passed!'
        }
    }
}

        stage('Code Quality Check') {
            when {
                expression { params.SKIP_TESTS == false }
            }
            steps {
                echo '📊 Running code quality checks...'
                script {
                    // Checkstyle validation using Sun Java coding standards
                    // Checks: Line length (80 chars), trailing spaces, indentation,
                    // naming conventions, Javadoc, code formatting
                    // Status: Non-blocking warnings (marked UNSTABLE on violations)
                    def services = ['user-service']
                    for (service in services) {
                        dir(service) {
                            catchError(buildResult: 'SUCCESS', stageResult: 'UNSTABLE') {
                                sh 'mvn checkstyle:check'
                            }
                        }
                    }
                    echo '✅ Code quality check completed'
                }
            }
            post {
                always {
                    // Archive code quality reports
                    archiveArtifacts artifacts: '**/target/checkstyle-result.xml', allowEmptyArchive: true
                }
            }
        }

        stage('Build Backend JARs') {
            steps {
                echo '📦 Building backend JAR files...'
                script {
                    def services = ['service-registry', 'user-service', 'product-service', 'media-service']
                    for (service in services) {
                        dir(service) {
                            sh 'mvn clean package -DskipTests'
                        }
                    }
                }
            }
        }

        stage('Build Docker Images') {
            steps {
                echo "🐳 Building Docker Images in parallel with tag: ${BUILD_VERSION}..."
                script {
                    parallel(
                        'service-registry': {
                            sh "docker build -t ${DOCKER_USER}/buy-01-service-registry:latest -t ${DOCKER_USER}/buy-01-service-registry:${BUILD_VERSION} ./service-registry"
                        },
                        'api-gateway': {
                            sh "docker build -t ${DOCKER_USER}/buy-01-api-gateway:latest -t ${DOCKER_USER}/buy-01-api-gateway:${BUILD_VERSION} ./api-gateway"
                        },
                        'user-service': {
                            sh "docker build -t ${DOCKER_USER}/buy-01-user-service:latest -t ${DOCKER_USER}/buy-01-user-service:${BUILD_VERSION} ./user-service"
                        },
                        'product-service': {
                            sh "docker build -t ${DOCKER_USER}/buy-01-product-service:latest -t ${DOCKER_USER}/buy-01-product-service:${BUILD_VERSION} ./product-service"
                        },
                        'media-service': {
                            sh "docker build -t ${DOCKER_USER}/buy-01-media-service:latest -t ${DOCKER_USER}/buy-01-media-service:${BUILD_VERSION} ./media-service"
                        },
                        'frontend': {
                            sh "docker build -t ${DOCKER_USER}/buy-01-frontend:latest -t ${DOCKER_USER}/buy-01-frontend:${BUILD_VERSION} ./buy-01-ui"
                        }
                    )
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                echo "🚀 Pushing images to Docker Hub in parallel..."
                withCredentials([usernamePassword(
                    credentialsId: REGISTRY_CREDENTIALS,
                    usernameVariable: 'DOCKER_USER_CRED',
                    passwordVariable: 'DOCKER_PASS'
                )]) {
                    sh 'echo $DOCKER_PASS | docker login -u $DOCKER_USER_CRED --password-stdin'
                    script {
                        def dockerUser = env.DOCKER_USER
                        def buildVer = env.BUILD_VERSION
                        parallel(
                            'service-registry': {
                                sh "docker push '${dockerUser}/buy-01-service-registry:latest'"
                                sh "docker push '${dockerUser}/buy-01-service-registry:${buildVer}'"
                            },
                            'api-gateway': {
                                sh "docker push '${dockerUser}/buy-01-api-gateway:latest'"
                                sh "docker push '${dockerUser}/buy-01-api-gateway:${buildVer}'"
                            },
                            'user-service': {
                                sh "docker push '${dockerUser}/buy-01-user-service:latest'"
                                sh "docker push '${dockerUser}/buy-01-user-service:${buildVer}'"
                            },
                            'product-service': {
                                sh "docker push '${dockerUser}/buy-01-product-service:latest'"
                                sh "docker push '${dockerUser}/buy-01-product-service:${buildVer}'"
                            },
                            'media-service': {
                                retry(3) {
                                    sh "docker push '${dockerUser}/buy-01-media-service:latest'"
                                }
                                sh "docker push '${dockerUser}/buy-01-media-service:${buildVer}'"
                            },
                            'frontend': {
                                sh "docker push '${dockerUser}/buy-01-frontend:latest'"
                                sh "docker push '${dockerUser}/buy-01-frontend:${buildVer}'"
                            }
                        )
                    }
                }
            }
        }

        stage('Deploy') {
            steps {
                echo "🚀 Deploying to ${params.DEPLOY_ENV} environment..."
                script {
                    // Helper function: Wait for container to be running
                    def waitForContainer = { String containerName, int maxAttempts ->
                        def attempt = 0
                        def isRunning = false
                        
                        while (attempt < maxAttempts && !isRunning) {
                            attempt++
                            echo "⏳ Checking ${containerName}... (${attempt}/${maxAttempts})"
                            
                            def status = sh(
                                script: "docker ps --filter name=${containerName} --format '{{.Status}}' | grep -q 'Up'",
                                returnStatus: true
                            )
                            
                            if (status == 0) {
                                echo "✅ ${containerName} is running"
                                isRunning = true
                            } else {
                                if (attempt < maxAttempts) {
                                    sleep(1)  // Reduced from 2 to 1 second
                                }
                            }
                        }
                        
                        if (!isRunning) {
                            error("❌ ${containerName} failed to start after ${maxAttempts} attempts")
                        }
                    }
                    
                    // Helper function: Wait for HTTP health endpoint
                    def waitForServiceHealth = { String healthUrl, int maxAttempts ->
                        def attempt = 0
                        def isHealthy = false
                        
                        while (attempt < maxAttempts && !isHealthy) {
                            attempt++
                            echo "⏳ Health check ${healthUrl}... (${attempt}/${maxAttempts})"
                            
                            def status = sh(
                                script: "curl -sf ${healthUrl} > /dev/null 2>&1",
                                returnStatus: true
                            )
                            
                            if (status == 0) {
                                echo "✅ Service is healthy!"
                                isHealthy = true
                            } else {
                                if (attempt < maxAttempts) {
                                    sleep(2)  // Reduced from 3 to 2 seconds
                                }
                            }
                        }
                        
                        if (!isHealthy) {
                            echo "⚠️ Warning: ${healthUrl} not responding (continuing anyway)"
                        }
                    }
                    
                    // Stop and remove all containers
                    sh '''
                        cd deployment
                        docker stop buy-01-zookeeper buy-01-mongodb buy-01-kafka buy-01-service-registry buy-01-api-gateway buy-01-user-service buy-01-product-service buy-01-media-service buy-01-frontend 2>/dev/null || true
                        docker rm buy-01-zookeeper buy-01-mongodb buy-01-kafka buy-01-service-registry buy-01-api-gateway buy-01-user-service buy-01-product-service buy-01-media-service buy-01-frontend 2>/dev/null || true
                        echo "✅ Using locally built images (already pushed to Docker Hub)"
                    '''
                    
                    // Start all infrastructure in parallel
                    echo "📦 Starting Infrastructure (Zookeeper, MongoDB, Kafka)..."
                    sh 'cd deployment && docker-compose up -d zookeeper mongodb kafka'
                    
                    // Wait for infrastructure in parallel (faster!)
                    parallel(
                        'zookeeper': { waitForContainer('buy-01-zookeeper', 20) },
                        'mongodb': { waitForContainer('buy-01-mongodb', 20) },
                        'kafka': { waitForContainer('buy-01-kafka', 30) }
                    )
                    
                    // Start service registry (needed by microservices)
                    echo "📦 Starting Service Registry..."
                    sh 'cd deployment && docker-compose up -d service-registry'
                    waitForServiceHealth('http://localhost:8761/actuator/health', 40)
                    
                    // Start all microservices in parallel
                    echo "📦 Starting Microservices in parallel..."
                    sh 'cd deployment && docker-compose up -d api-gateway user-service product-service media-service frontend'
                    
                    // Wait for critical services only
                    waitForServiceHealth('http://localhost:8080/actuator/health', 40)
                    
                    // Final verification
                    echo "🔍 Running final health checks..."
                    def healthCheck = sh(
                        script: '''
                            HEALTHY=true
                            if ! docker ps | grep -q "buy-01-service-registry"; then
                                echo "❌ service-registry is not running"
                                HEALTHY=false
                            fi
                            if ! docker ps | grep -q "buy-01-api-gateway"; then
                                echo "❌ api-gateway is not running"
                                HEALTHY=false
                            fi
                            if [ "$HEALTHY" = "false" ]; then
                                exit 1
                            fi
                            echo "✅ All services are running and healthy!"
                        ''',
                        returnStatus: true
                    )
                    
                    if (healthCheck != 0) {
                        error('❌ Deployment health check failed!')
                    }
                }
            }
        }

        stage('Rollback on Failure') {
            when {
                expression { currentBuild.result == 'FAILURE' || currentBuild.result == 'UNSTABLE' }
            }
            steps {
                script {
                    echo "🔄 Rolling back to previous version: ${PREVIOUS_BUILD}..."
                    
                    // Helper function for container wait
                    def waitForContainer = { containerName, maxAttempts = 30 ->
                        def attempts = 0
                        while (attempts < maxAttempts) {
                            def running = sh(
                                script: "docker ps --filter name=${containerName} --filter status=running -q",
                                returnStdout: true
                            ).trim()
                            if (running) {
                                echo "✅ ${containerName} is running"
                                return true
                            }
                            attempts++
                            sleep 1
                        }
                        return false
                    }
                    
                    // Stop all services
                    sh 'cd deployment && docker-compose down || true'
                    
                    // Parallel image pull for speed
                    echo "📦 Pulling previous build images in parallel..."
                    parallel(
                        'service-registry': {
                            sh "docker pull hussainsaddam/buy-01-service-registry:${PREVIOUS_BUILD} || echo 'Image not found'"
                            sh "docker tag hussainsaddam/buy-01-service-registry:${PREVIOUS_BUILD} hussainsaddam/buy-01-service-registry:latest || true"
                        },
                        'api-gateway': {
                            sh "docker pull hussainsaddam/buy-01-api-gateway:${PREVIOUS_BUILD} || echo 'Image not found'"
                            sh "docker tag hussainsaddam/buy-01-api-gateway:${PREVIOUS_BUILD} hussainsaddam/buy-01-api-gateway:latest || true"
                        },
                        'user-service': {
                            sh "docker pull hussainsaddam/buy-01-user-service:${PREVIOUS_BUILD} || echo 'Image not found'"
                            sh "docker tag hussainsaddam/buy-01-user-service:${PREVIOUS_BUILD} hussainsaddam/buy-01-user-service:latest || true"
                        },
                        'product-service': {
                            sh "docker pull hussainsaddam/buy-01-product-service:${PREVIOUS_BUILD} || echo 'Image not found'"
                            sh "docker tag hussainsaddam/buy-01-product-service:${PREVIOUS_BUILD} hussainsaddam/buy-01-product-service:latest || true"
                        },
                        'media-service': {
                            sh "docker pull hussainsaddam/buy-01-media-service:${PREVIOUS_BUILD} || echo 'Image not found'"
                            sh "docker tag hussainsaddam/buy-01-media-service:${PREVIOUS_BUILD} hussainsaddam/buy-01-media-service:latest || true"
                        },
                        'frontend': {
                            sh "docker pull hussainsaddam/buy-01-frontend:${PREVIOUS_BUILD} || echo 'Image not found'"
                            sh "docker tag hussainsaddam/buy-01-frontend:${PREVIOUS_BUILD} hussainsaddam/buy-01-frontend:latest || true"
                        }
                    )
                    
                    // Restart services with rollback images
                    echo "🚀 Starting services with previous version..."
                    sh 'cd deployment && docker-compose up -d --remove-orphans'
                    
                    // Verify rollback health
                    echo "🏥 Verifying rollback health..."
                    def rollbackHealthy = true
                    
                    if (!waitForContainer('buy-01-service-registry', 30)) {
                        echo "⚠️ service-registry failed to start after rollback"
                        rollbackHealthy = false
                    }
                    
                    if (!waitForContainer('buy-01-api-gateway', 30)) {
                        echo "⚠️ api-gateway failed to start after rollback"
                        rollbackHealthy = false
                    }
                    
                    if (rollbackHealthy) {
                        echo "✅ Rollback to build ${PREVIOUS_BUILD} completed successfully"
                    } else {
                        echo "⚠️ Rollback completed but some services are unhealthy"
                        echo "💡 Check logs: docker-compose -f deployment/docker-compose.yml logs"
                    }
                }
            }
        }
    }

    post {
        success {
            echo "✅ Pipeline completed successfully!"
            echo "Build #${env.BUILD_NUMBER} - All stages passed"
            
            // Slack notification
            script {
                try {
                    withCredentials([string(credentialsId: 'slack-webhook-url', variable: 'SLACK_URL')]) {
                        sh '''
                            curl -X POST -H 'Content-type: application/json' \
                            --data "{\\"text\\":\\"✅ *SUCCESS*: Job ${JOB_NAME} Build #${BUILD_NUMBER}\\\\n${BUILD_URL}\\"}" \
                            ${SLACK_URL}
                        '''
                    }
                } catch (Exception e) {
                    echo "⚠️ Slack notification skipped (credential not configured): ${e.message}"
                }
            }
        }
        failure {
            echo "❌ Pipeline failed!"
            echo "Build #${env.BUILD_NUMBER} - Check logs for details"
            
            // Slack notification
            script {
                try {
                    withCredentials([string(credentialsId: 'slack-webhook-url', variable: 'SLACK_URL')]) {
                        sh '''
                            curl -X POST -H 'Content-type: application/json' \
                            --data "{\\"text\\":\\"❌ *FAILED*: Job ${JOB_NAME} Build #${BUILD_NUMBER}\\\\n${BUILD_URL}console\\"}" \
                            ${SLACK_URL}
                        '''
                    }
                } catch (Exception e) {
                    echo "⚠️ Slack notification skipped (credential not configured): ${e.message}"
                }
            }
        }
        always {
            echo "🧹 Cleaning up workspace..."
            // Archive build artifacts before cleanup
            archiveArtifacts artifacts: '**/target/*.jar', allowEmptyArchive: true, fingerprint: true
            cleanWs()
        }
    }
}
